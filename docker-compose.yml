version: '3.3'

services:
  weather-app:
    image: node:12-buster-slim
    container_name: wheater_comparitor_app
    working_dir: /home/wheater_app/
    volumes:
      - ./:/home/wheater_app/
    ports:
      - 5000:5000
    stdin_open: true
    tty: true
    entrypoint:
      - bash
